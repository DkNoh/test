<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sms.mapper.MenuMapper">

    <!-- 
        [UI용 쿼리] 권한별 접근 가능한 메뉴 목록 조회 
        NOTE: TB_MENU와 TB_MENU_AUTH를 조인하여 사용자에게 허가된 '사용 중(Y)'인 메뉴만 가져옵니다.
    -->
    <select id="selectMenusByAuthCode" resultType="com.example.sms.dto.MenuDTO">
        SELECT 
            A.MENU_CD,
            A.MENU_NM,
            A.MENU_URL,
            A.UP_MENU_CD,
            A.SORT_ORD,
            A.USE_YN
        FROM TB_MENU A
        INNER JOIN TB_MENU_AUTH B ON A.MENU_CD = B.MENU_CD
        WHERE A.USE_YN = 'Y'
          AND B.AUTH_CD = #{authCd, jdbcType=VARCHAR}
        ORDER BY A.UP_MENU_CD ASC NULLS FIRST, A.SORT_ORD ASC
    </select>

    <!-- 
        [보안용 쿼리] 인터셉터용 URL 권한 실시간 검증 
        NOTE: 주소창에 직접 URL을 치고 들어왔을 때 해당 URL이 현재 권한과 매핑되어 있는지 카운트합니다.
    -->
    <select id="countAuthForUrl" resultType="int">
        SELECT COUNT(1)
        FROM TB_MENU A
        INNER JOIN TB_MENU_AUTH B ON A.MENU_CD = B.MENU_CD
        WHERE A.USE_YN = 'Y'
          AND A.MENU_URL = #{menuUrl, jdbcType=VARCHAR}
          AND B.AUTH_CD = #{authCd, jdbcType=VARCHAR}
    </select>

</mapper>
