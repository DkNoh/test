# 필수 변경 작업

> BASE 프로젝트를 실제 개발/폐쇄망/운영 환경으로 넘기기 전에 반드시 확인하거나 수정해야 하는 항목입니다.

## 1. 운영 인증 전환

- 현재 `prod` 프로파일의 인증은 LDAP 연동 전 임시 구현이다.
- 실제 개발/운영 반영 전 `SecurityConfig`의 `prodUserDetailsService` 임시 인메모리 계정을 제거하고 사내 LDAP/AD 인증으로 교체한다.
- 임시 `admin/admin1234` 계정이 운영 프로파일에서 활성화되지 않도록 확인한다.

## 2. 테스트/개발 전용 초기화 코드 정리

- `DataInsertTest`와 `TableInitRunner`는 제거했다.
- 루트와 `scratch/`의 일회성 DB 초기화/더미 insert 파일도 제거했다.
- 실제 운영/폐쇄망 이관 시 런타임 DDL, 더미 데이터 삽입, 예외 무시 초기화 로직이 다시 추가되지 않도록 한다.

## 3. 예외 응답 메시지 정리

- `GlobalExceptionHandler`의 일반 예외 응답은 현재 내부 예외 메시지를 함께 내려줄 수 있다.
- 운영 반영 전에는 사용자 응답에 내부 예외 메시지, SQL, 경로, 클래스명 등이 노출되지 않도록 정리한다.
- 상세 원인은 서버 로그에만 남기고, 화면/API 응답은 표준 에러 코드와 일반 메시지로 제한한다.

## 4. 스캐폴드 local 전용 유지

- `SystemScaffoldController`는 `@Profile("local")` 전용으로 유지한다.
- 개발서버(dev)와 운영서버(prod)에는 `/system/scaffold`가 노출되지 않아야 한다.
- 스캐폴드로 생성한 결과물만 검토 후 업무 프로젝트에 반영한다.
- 스캐폴드 생성기 자체를 서버 배포 산출물에 포함할지 여부는 보안 기준에 따라 별도 승인받는다.

## 5. 문서 자동 동기화

- `docs/api-mapping.md`는 현재 코드와 일치하는 문서로 만들다 중단된 상태다.
- 신규 화면/Controller/Template/JS/Mapper 추가 시 `docs/api-mapping.md`와 `docs/menu-structure.md`가 자동 또는 반자동으로 갱신되는 로직이 필요하다.
- 스캐폴드 생성기에서 파일 생성 결과와 메뉴 등록 SQL을 반환할 때 문서 갱신용 스니펫도 함께 생성하도록 개선한다.
- 최소 기준: URL, Controller, Template, JS, 데이터 API, 조회조건, 그리드 필드, 구현 상태를 한 번에 갱신할 수 있어야 한다.

## 6. Referer 기반 권한 허용 검토

- `MenuAuthInterceptor`에는 직접 URL 권한이 없어도 `Referer` 헤더의 부모 화면에 권한이 있으면 하위 API 호출을 허용하는 로직이 있다.
- 이 방식은 화면-하위 API 연결이 편해지는 장점이 있지만, `Referer` 헤더는 클라이언트가 보내는 값이라 보안 판단의 근거로 쓰기에는 약하다.
- BASE 프로젝트로 재사용하기 전에는 `Referer` 허용을 제거하고, `/data`, `/save`, `/delete`, `/excel` 같은 하위 API를 부모 메뉴 URL로 명시적으로 매핑하는 방식으로 전환하는 것을 검토한다.

## 7. 실제 DB 필드 매핑 문서화

- 현재 화면 필드, Java 필드, DB 컬럼은 폐쇄망 레거시 화면 관찰과 AI naming을 기반으로 한 임시 계약이다.
- 실제 DB 메타데이터 확보 후 `화면 필드 -> Java 필드 -> 실제 테이블/컬럼 -> 상태(추정/확인)` 매핑 문서를 작성한다.
- Java 변경을 최소화하기 위해 우선 MyBatis XML의 SQL alias로 실제 DB 컬럼과 Java 필드를 연결한다.

## 8. 감사 로그 적용 범위 점검

- `docs/audit-log-policy.md` 기준에 따라 개인정보 조회, 엑셀 다운로드, 권한 변경, 승인/반려, 발송/예약 기능에 `@PrivacyLog` 적용 여부를 점검한다.
- 스캐폴드 생성기에 개인정보 포함 여부 옵션을 추가할지 검토한다.
- 운영 반영 전 감사 로그에 원문 개인정보가 저장되지 않는지 확인한다.
